# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtualenv
.venv/
venv/
env/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# Build artifacts (rebuilds via gcc vendor/QL6502-src/*.c)
bin/ql6502
build/
*.o

# Local test ROMs and analysis outputs (not redistributable)
roms/
out/
*.nes
*.zip

# ROM-derived artifacts (do NOT commit — copyright/DMCA risk on commercial ROMs)
# These are regenerated locally by `qlnes analyze` / `qlnes recompile` from a
# user-supplied ROM and must never be redistributed.
STACK.md
*.bank*.asm
smb*.asm
assets/

# Local Claude Code configuration and skill installs (re-installable via
# `npx bmad-method install ...` — not part of the project source).
.claude/
